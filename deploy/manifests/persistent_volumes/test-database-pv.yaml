apiVersion: v1
kind: PersistentVolume
metadata:
  name: test-database-pv
  namespace: test
  labels:
    app: gitrello
    env: test
spec:
  storageClassName: test
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/test/data"
